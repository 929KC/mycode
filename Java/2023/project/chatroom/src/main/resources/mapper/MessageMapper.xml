<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kc.chatroom.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.kc.chatroom.model.entity.Message">
            <id property="messageId" column="messageId" jdbcType="INTEGER"/>
            <result property="fromId" column="fromId" jdbcType="INTEGER"/>
            <result property="sessionId" column="sessionId" jdbcType="INTEGER"/>
            <result property="content" column="content" jdbcType="VARCHAR"/>
            <result property="postTime" column="postTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        messageId,fromId,sessionId,
        content,postTime
    </sql>
    <select id="getLastMessagesBySessionId" resultType="java.lang.String">
        select content from message where sessionId = #{sessionId} order by post
    </select>
    <select id="getMessagesBySessionId" resultType="com.kc.chatroom.model.entity.Message">

    </select>
</mapper>
